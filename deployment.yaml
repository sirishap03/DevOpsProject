kind: Deployment
apiVersion: apps/v1
metadata:
  name: deployment_file
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata: 
      labels:
        app: myapp
    spec:
      containers:
      - name: mycontainer
        image: <img-name>   # replace with your Docker-image pushed on Dockerhub
        ports: 
        - containerPort: 5000